# Ignore everything at repo root except frontend, backend, README.md (and this file)
*
!.gitignore
!/frontend/
!/backend/
!/README.md

# Environment and secrets (API keys) — ignored everywhere
.env

# Cursor IDE
.cursor